package com.tt;

/**
 * Unit test for simple App.
 */
//@RunWith(SpringRunner.class)
//@ContextConfiguration({"classpath:/spring/app-context.xml"})
public class AppTest {
//    @Autowired
//    private AccountRepository accountRepository;
//
//    private AccountService accountService;
//
//    @Before
//    public void setUp() {
//        accountService = new AccountService(accountRepository);
//    }
//
//
//    @Test
//    public void test_verifier() {
//        System.out.println(accountRepository);
//        accountRepository.findById(5);
//        Assert.assertEquals("OK", accountService.resolveCommand("NEWACCOUNT 44444"));
//        Assert.assertEquals("OK", accountService.resolveCommand("DEPOSIT 44444 60.5"));
//        Assert.assertEquals("OK", accountService.resolveCommand("WITHDRAW 44444 0.5"));
//        Assert.assertEquals("OK", accountService.resolveCommand("DEPOSIT 44445"));
//        Assert.assertEquals("OK", accountService.resolveCommand("BALANCE 44444"));
//        Assert.assertEquals("OK", accountService.resolveCommand("BALANCE 44445"));
//    }
}
